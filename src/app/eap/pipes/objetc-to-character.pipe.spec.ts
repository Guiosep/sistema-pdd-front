import { ObjetcToCharacterPipe } from './objetc-to-character.pipe';

describe('ObjetcToCharacterPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjetcToCharacterPipe();
    expect(pipe).toBeTruthy();
  });
});
